# 🔄 版本回滚说明

**回滚时间**: 2026-03-12 14:55  
**回滚目标**: 移除今天添加的所有非核心功能，恢复到最简洁版本

---

## 📦 今天添加的功能（需移除）

### 1. 移动端适配 CSS
- 所有页面的 `@media` 查询
- 响应式布局代码

### 2. 返回首页链接
- 所有子页面的 `🏠 首页` 按钮

### 3. 日本地图
- `web/japan-map.html`
- 数据库中的日本数据（可选保留）

### 4. 参数配置页面
- `web/config.html`

### 5. 数字孪生 3D 修复
- WebGL 检测代码
- Canvas 样式优化

---

## ✅ 保留的核心功能

### 必须保留
- ✅ 中国投资地图（map-v1.0.html）
- ✅ 越南投资地图（vietnam-map.html）
- ✅ 首页（index.html）
- ✅ 后端 API 服务
- ✅ 数据库
- ✅ 登录/管理后台

---

## 🔧 回滚方案

由于没有版本控制系统，建议采用以下方案：

### 方案 1：手动清理（推荐）

逐文件移除今天添加的代码：

1. **移除移动端 CSS**
   ```bash
   # 删除所有 @media 查询代码
   ```

2. **移除返回首页链接**
   ```bash
   # 删除所有 🏠 首页 按钮
   ```

3. **删除新增页面**
   ```bash
   rm web/japan-map.html
   rm web/config.html
   ```

### 方案 2：重新部署（最干净）

如果有原始代码包：
```bash
# 1. 备份当前数据
cp backend/data/solar-storage.db /backup/

# 2. 重新解压原始代码
rm -rf web/ backend/
tar -xzf china-solar-storage-original.tar.gz

# 3. 恢复数据
cp /backup/solar-storage.db backend/data/
```

---

## 📋 建议

**由于没有版本控制，建议：**

1. **立即建立 git 仓库**
   ```bash
   cd /home/admin/openclaw/workspace/projects/china-solar-storage
   git init
   git add .
   git commit -m "Current state"
   ```

2. **定期备份**
   ```bash
   tar -czf backup-$(date +%Y%m%d).tar.gz \
     --exclude='node_modules' \
     --exclude='data/*.db' \
     .
   ```

3. **使用配置管理**
   - 记录所有修改
   - 使用分支开发新功能
   - 测试通过后再合并

---

## ⚠️ 当前状态

**目前没有简单的方法一键回滚**，因为：
- ❌ 没有 git 历史
- ❌ 没有备份文件
- ❌ 所有修改都在工作目录

**建议方案**：
1. 手动清理不需要的功能
2. 或者提供原始代码包重新部署

---

请告诉我您想要：
1. **手动清理** - 我帮您逐个文件移除今天的修改
2. **重新部署** - 如果您有原始代码包
3. **保留现状** - 只修复 critical bug，不进行大的改动
